[project]
name = "projeto"
version = "0.1.0"
description = "Sistema distribuido de rastreamento de entregas"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pika",
    "pydantic",
    "httpx",
]
